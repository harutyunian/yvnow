export enum TodayButtons {
    all = 'All',
    event = 'Event',
    show = 'Show',
    concert = 'Concert',
}
export enum FilterAction {
    all = "all",
    today = "today",
    upcoming = 'upcoming',
    live = 'live'
}
